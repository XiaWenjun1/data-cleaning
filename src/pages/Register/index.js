import { Form, Input, Button } from 'antd';
import './index.scss';

const Register = () => {
    const onFinish = (values) => {
        console.log(values);
    };

    return (
        <div>这是一个页面</div>
    );

}

export default Register;